name: Request CodeRabbit Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: request-coderabbit-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  pull-requests: write

jobs:
  request-review:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Request CodeRabbit and Gemini reviews
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea # v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const prNumber = context.payload.pull_request.number;
            try {
              await github.rest.pulls.requestReviewers({
                owner,
                repo,
                pull_number: prNumber,
                reviewers: ['coderabbitai', 'gemini-code-assist[bot]'],
              });
              core.info('Review requested from coderabbitai and gemini-code-assist[bot]');
            } catch (e) {
              const status = e.response?.status;
              const msg = e.response?.data?.message ?? e.message;
              if (status === 422) {
                core.warning(`Reviewer request returned 422 (benign): ${msg}`);
              } else {
                core.error(`Reviewer request failed: ${status ?? ''} ${msg}`);
                throw e;
              }
            }
