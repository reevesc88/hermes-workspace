interface:
  display_name: "Hermes Workspace"
  short_description: "Repo-specific patterns and workflows for hermes-workspace"
  default_prompt: "Use the hermes-workspace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true